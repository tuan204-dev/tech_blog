// import getAllPosts from '@/'

export default async function Home() {
  // const posts = await getAllPosts({ page: 1 })

  return (
    <div className="dark:text-white">
      {/* <Pagination count={Math.ceil(postsQuantity / postsPerPage)} /> */}
    </div>
  )
}
